<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Edit Product - Inventory A+</title>
    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>

<div class="d-flex">
    <%- include('partials/sidebar') %>

    <div class="container-fluid p-4">
        <div class="card shadow-sm" style="max-width: 600px; margin: auto;">
            <div class="card-header bg-warning">
                <h4 class="mb-0">Edit Product</h4>
            </div>
            <div class="card-body">
                <form action="/products/edit/<%= product._id %>" method="POST">
                   <div class="mb-3">
                        <label>Image URL</label>
                        <input type="text" name="image" value="<%= product.image %>" class="form-control" placeholder="Paste image link here">
                   </div>
                    <div class="mb-3">
                        <label>Name</label>
                        <input type="text" name="name" value="<%= product.name %>" class="form-control" required>
                    </div>
                        <label>Category</label>
                        <select name="category" class="form-select" required>
                            <option value="" disabled selected>Select a Category</option>
                            <% if(categories.length > 0) { %>
                                <% categories.forEach(cat => { %>
                                    <option value="<%= cat.name %>"><%= cat.name %></option>
                                <% }) %>
                            <% } else { %>
                                <option value="General">General (No categories created yet)</option>
                            <% } %>
                        </select>
                    </div>
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label>Price</label>
                            <input type="number" name="price" value="<%= product.price %>" class="form-control" required>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label>Quantity</label>
                            <input type="number" name="quantity" value="<%= product.quantity %>" class="form-control" required>
                        </div>
                    </div>
                    <button type="submit" class="btn btn-primary">Update Product</button>
                    <a href="/products" class="btn btn-secondary">Cancel</a>
                </form>
            </div>
        </div>
    </div>
</div>

</body>
</html>