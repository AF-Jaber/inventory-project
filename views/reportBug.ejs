<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Report a Bug</title>
    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>

<div class="d-flex">
    <%- include('partials/sidebar') %>

    <div class="container-fluid p-4">
        <h2>Report a Bug</h2>
        <p class="text-muted">Found an issue? Describe it below and our team will fix it.</p>

        <!-- Success/Error Message -->
        <% if (message) { %>
            <div class="alert alert-warning"><%= message %></div>
        <% } %>

        <div class="card shadow-sm" style="max-width: 600px;">
            <div class="card-body">
                <form action="/report-bug" method="POST">
                    <div class="mb-3">
                        <label class="form-label">Description of the issue</label>
                        <textarea name="description" class="form-control" rows="6" placeholder="e.g. The add product button is not working..." required></textarea>
                    </div>
                    <button type="submit" class="btn btn-danger">
                        <i class="fas fa-paper-plane"></i> Submit Report
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>

</body>
</html>